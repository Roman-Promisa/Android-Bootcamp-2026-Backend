<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="2026-01-28--0003-meeting" author="anepretimov">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="meeting"/>
            </not>
        </preConditions>

        <createTable tableName="meeting">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="title" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="TEXT"/>
            <column name="organizer_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_meeting_organizer"
                             referencedTableName="person"
                             referencedColumnNames="id"/>
            </column>
            <column name="meeting_date" type="DATE">
                <constraints nullable="false"/>
            </column>
            <column name="start_time" type="TIME">
                <constraints nullable="false"/>
            </column>
            <column name="end_time" type="TIME">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="VARCHAR(20)" defaultValue="PLANNED">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex tableName="meeting" indexName="idx_meeting_organizer">
            <column name="organizer_id"/>
        </createIndex>
        <createIndex tableName="meeting" indexName="idx_meeting_date">
            <column name="meeting_date"/>
        </createIndex>
        <createIndex tableName="meeting" indexName="idx_meeting_status">
            <column name="status"/>
        </createIndex>

        <!-- Добавляем проверку на часовые интервалы -->
        <sql>
            ALTER TABLE meeting ADD CONSTRAINT chk_meeting_hourly
            CHECK (
            EXTRACT(MINUTE FROM start_time) = 0
            AND EXTRACT(SECOND FROM start_time) = 0
            AND EXTRACT(MINUTE FROM end_time) = 0
            AND EXTRACT(SECOND FROM end_time) = 0
            AND end_time > start_time
            );
        </sql>
    </changeSet>

</databaseChangeLog>